# --- sessions 1, 6, 7 (leakage, DVC, MLflow) ---
scikit-learn
pandas
numpy
matplotlib
pyyaml
dvc>=3.0
dvclive>=3.0
mlflow>=2.9
psutil            # optional: MLflow system metrics

# --- session 3 (production API) ---
fastapi
pydantic>=2
uvicorn
requests
httpx

# --- session 5 (ETL) ---
sqlalchemy
apache-airflow>=3.3       # install with the official constraints file for your Python:
#   pip install "apache-airflow==3.3.2" \
#     --constraint https://raw.githubusercontent.com/apache/airflow/constraints-3.3.2/constraints-3.12.txt
# Note: airflow-core pins fastapi<0.137, which caps session 3's FastAPI in a shared
# environment. Both sessions work at that version; it is a ceiling, not a conflict.

# --- session 2 (deep learning) ---
torch
transformers
accelerate
onnx
onnxruntime

# --- session 8 (distributed ML) ---
ray[train]

# --- session 9 (feature management) ---
feast

# --- sessions 10 and 13 (monitoring, and the API that exposes /metrics) ---
prometheus-client

# --- session 11 (automation) ---
ray[serve]

# --- session 13 (end to end) ---
joblib

# Not installable with pip, and needed by some sessions:
#   Docker Engine + compose  (sessions 4, 10, 13)
#   Terraform CLI            (session 12)
#   git                      (sessions 6, 7, 13)
